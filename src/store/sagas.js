/**
 * uncomment to import and register all sagas functions
 */
/*
import { all, fork } from 'redux-saga/effects'

const rootSaga = []


export default function*() {
    yield all(rootSaga.map(saga => fork(saga)))
}

*/